<p id="notice"><%= notice %></p>

<section class="content">
  
  <b>Project name:</b>
  <%= @project.projectName %>

  <b>Category:</b>
  <%= @project.category %>

  <b>Thumbnail:</b>
  <%= image_tag @project.thumbnail(:thumb) %>
  
  <b>Web One Up:</b>
  <iframe src="<%= @project.webOneUp %>" />
</section>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
